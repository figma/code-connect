import React from 'react'

interface ButtonProps {
  width: string | undefined
  variant: string
  disabled: boolean
}

export function Button({ width, variant, disabled }: ButtonProps) {
  return <button />
}
