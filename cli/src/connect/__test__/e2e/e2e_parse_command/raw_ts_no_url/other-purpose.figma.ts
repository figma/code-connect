// A .figma.ts file used for a purpose other than Code Connect (e.g. Make's code
// component property definitions). It has no // url= directive and contains the
// string `codeProperties`, so the CLI logs and skips it instead of failing.
// component=Button
// source=src/button.tsx

export const codeProperties = {
  name: 'not-code-connect',
  value: 'value',
}
